V1.1 产品调整

核心筛选：
1. 校区
2. 预算
3. 食物类型

弱化筛选：
4. 不吃辣（放入更多条件）

移除：
5. 原辣度等级筛选

设计目标：
减少用户选择成本，让用户 5 秒内完成一次推荐。

V1.4 数据模型：

每道菜都包含 `school`、`campus`、`type` 等归属字段。当前数据全部属于中山大学，因此页面体验不变；以后新增其他学校时，可先按 `school` 区分数据，再增加学校筛选控件。
